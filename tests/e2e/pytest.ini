[pytest]
# E2E tests run against a live instance — they do NOT import app code.
# This confcutdir ensures the root tests/conftest.py (which imports app
# modules requiring Python 3.11+) is not loaded.
confcutdir = .
timeout = 120
