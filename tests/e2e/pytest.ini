[pytest]
# E2E tests run against a live instance — they do NOT import app code.
# This rootdir prevents pytest from loading the root tests/conftest.py
# (which imports app modules requiring Python 3.11+).
timeout = 120
