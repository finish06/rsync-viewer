# Dev dependencies — extends requirements.txt
-r requirements.txt

# Linting — pinned; newer ruff releases change default rules (see ci.yml)
ruff==0.15.2

# Type checking
mypy>=1.8.0

# Testing (already in requirements.txt, pinned here for clarity)
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
httpx>=0.27.0
