# Retrospective — 2026-02-24

**Project:** rsync-viewer
**Period:** 2026-02-23 → 2026-02-24
**Milestones:** M6 Observability (COMPLETE)

## Period Summary

- Specs completed: 2 (documentation, metrics-export)
- TDD cycles run: 2 (cycle-5: metrics/retention, cycle-6: docs/Grafana)
- Commits: 22 across 4 branches
- PRs merged: 4 (#9, #10, #11, #12)
- Tests: 350 → 403 (15% growth, then back to 403 after e2e removal)
- Coverage: 91% sustained
- Release tags: v1.5.0 → v1.7.0
- Learnings recorded: 3 new (L-012, L-013, L-014)

## Human Perspective

**What went well:**
- TDD + away mode workflow continues to deliver reliably
- Milestone velocity — M6 completed in 2 focused cycles with clean splits

**What was frustrating:**
- Playwright event loop contamination debugging (significant time, no feature value)
- CI Docker mount drift (broke CI for new directories, happened twice)
- CI failures not being fixed before requesting merge — PRs were created with known failures

**Agent collaboration feedback:**
- Should verify CI passes before creating PRs or requesting merge

**Previous retro follow-up:**
- Ruff format before commit: partially helped (no formatting drift, but CI still had issues)
- Defer Playwright to Beta: partially helped (still caused event loop issues despite deferral)

## Agent Observations

**Velocity:**
- M6 completed in 2 cycles over 2 days
- 53 new tests (26 metrics + 27 docs)
- Fastest: Grafana dashboards (~30 min)
- Slowest: Playwright event loop debugging (~2 hours, no feature value)

**Spec Quality:**
- 0 blocks from ambiguous specs
- 0 mid-implementation revisions needed

**Quality Gates:**
- First-pass rate: 75% (3/4 passed first try)
- Failure: CI tests failed due to missing Docker volume mounts
- PR #11 created before CI verified — required follow-up fix commit

**Patterns:**
- CI failures should block PR creation
- Docker volume mounts are a recurring blind spot
- 2-cycle milestone splits deliver consistently well

## Agreed Changes

1. **CI must pass before PR** — run full CI checks and verify passes before creating PRs or requesting merge. Never create a PR with known failures.
2. **Fix Docker mount pattern** — mount entire project root in docker-compose.dev.yml to eliminate recurring mount drift for new directories.
3. **Drop Playwright entirely** — remove e2e tests from the project. They caused repeated issues (event loop contamination, CI complexity) with no proportional value at this maturity stage.

## Actions Taken

- Removed `tests/e2e/` directory (Playwright tests and conftest)
- Simplified `docker-compose.dev.yml` to mount project root (`. → /app:ro`)
- Removed e2e marker from `pytest.ini`
- Promoted maturity: Alpha → Beta (evidence score 7/7)

## Maturity Promotion

**Alpha → Beta** — Evidence score 7/7:
- 19 feature specs
- 91% test coverage
- CI/CD pipeline passing
- PR workflow with 12 merged PRs
- Tier 2 environments (local + production)
- Conventional commits (27/30)
- TDD evidence across all milestones

New Beta rules now active: TDD enforcement, agent coordination, environment awareness.
