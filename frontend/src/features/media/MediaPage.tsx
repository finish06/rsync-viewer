import { Panel } from "../../components/Panel";

/** Placeholder until cycle C5 (AC-019). */
export function MediaPage() {
  return (
    <Panel title="Media" testId="media-page">
      <p className="text-sm text-muted">
        New shows and movies arrive in cycle C5.
      </p>
    </Panel>
  );
}
