import { Panel } from "../../components/Panel";

/** Placeholder until cycle C5 (AC-004). */
export function UptimePage() {
  return (
    <Panel title="Uptime" testId="uptime-page">
      <p className="text-sm text-muted">
        Synthetic check history arrives in cycle C5.
      </p>
    </Panel>
  );
}
