import { Panel } from "../../components/Panel";

/** Placeholder until cycle C4 (AC-013–AC-015). */
export function TrendsPage() {
  return (
    <Panel title="Trends" testId="trends-page">
      <p className="text-sm text-muted">
        Transfers-over-time charts arrive in the next cycle.
      </p>
    </Panel>
  );
}
